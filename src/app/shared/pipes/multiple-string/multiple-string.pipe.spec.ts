import { MultipleStringPipe } from './multiple-string.pipe';

describe('MultipleStringPipe', () => {
  it('Create an instance', () => {
    const pipe = new MultipleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
