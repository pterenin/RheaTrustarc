import { DataElementPipe } from './data-element.pipe';

describe('DataElementPipe', () => {
  it('Create an instance', () => {
    const pipe = new DataElementPipe();
    expect(pipe).toBeTruthy();
  });
});
