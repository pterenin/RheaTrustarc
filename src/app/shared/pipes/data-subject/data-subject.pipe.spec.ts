import { DataSubjectPipe } from './data-subject.pipe';

describe('DataSubjectPipe', () => {
  it('Create an instance', () => {
    const pipe = new DataSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
