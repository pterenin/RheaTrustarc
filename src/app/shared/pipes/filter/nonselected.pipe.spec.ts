import { NonselectedPipe } from './nonselected.pipe';

describe('NonselectedPipe', () => {
  it('create an instance', () => {
    const pipe = new NonselectedPipe();
    expect(pipe).toBeTruthy();
  });
});
