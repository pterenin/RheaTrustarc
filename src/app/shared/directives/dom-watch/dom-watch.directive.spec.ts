import { TestBed } from '@angular/core/testing';
import { DomWatchDirective } from './dom-watch.directive';

describe('DomWatchDirective', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [DomWatchDirective]
    });
  });
});
